import 'package:flutter/material.dart';

class MeatCategoryScreen extends StatelessWidget {
  const MeatCategoryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: SafeArea(child: Center(child: Text('Meat Category screen'),),),);
  }
}
