import 'package:flutter/material.dart';
class GreenCategoryScreen extends StatelessWidget {
  const GreenCategoryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: SafeArea(child: Center(child: Text('Green Category screen'),),),);
  }
}
